//CONFLICTO AL HACER PULL REQUEST A DESARROLLO

git fetch origin
git checkout -b desarrollo-adrian-test origin/desarrollo-adrian //Crea una nueva rama donde vamos a solucionar los conflictos
git merge desarrollo //rama a la que queremos mergear los cambios

Solucionar los conflictos en los archivos afectados.

git add .
git commit -m ""
git checkout desarrollo 
git merge --no-ff desarrollo-adrian-test //mergear la rama test en la de desarrollo --no-ff significa 'no fast forward'

git push origin desarrollo